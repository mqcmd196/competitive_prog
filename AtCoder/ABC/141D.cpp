#include <bits/stdc++.h>
using namespace std;
 
int main(){
    int N, M;
    cin >> N;
    cin >> M;

    vector<long> A(N);
    for(int i=0; i<N; i++){
        cin >> A[i];
    }

    long max_value;

    //方針:とにかくでかいものを2で割っていく．それの繰り返し
    //ソートした配列を新しく作るべきか？
    for(int i=0; i<N; i++){
        
    }

    return 0;
}