#include <bits/stdc++.h>
using namespace std;
 
int main(){

    int N;
    vector<int> a, b;
    cin >> N;
    for(int i=0; i<N; i++){
        cin >> a.at(i);
        cin >> b.at(i);
    }

    return 0;
}